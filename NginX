server {
    // Add the code below to server section
    if($request_uri !～* "\.(jpg|png|gif|ico|css|js|html|htm|woff|woff2|eot|svg|ttf|map)$") {
        rewrite ^((?!index\.php).+)$    /index.php?$1;
    }
}